spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://192.168.3.5:3306/mall_ums
  cloud:
    nacos:
      server-addr: localhost:8848
    service-registry:
      auto-registration:
        enabled: true
    loadbalancer:
      ribbon:
        enabled: false
  application:
    name: member
mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 8082