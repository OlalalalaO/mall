spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://192.168.147.133:3306/mall_wms
  cloud:
    nacos:
      server-addr: localhost:8848
    service-registry:
      auto-registration:
        enabled: true
  application:
    name: ware
mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 8085